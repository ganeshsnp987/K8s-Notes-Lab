apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: default
  labels:
    app: mysql
spec:
  clusterIP: None   # 👈 Headless service (no cluster IP)
  ports:
  - port: 3306
    targetPort: 3306
    name: mysql
  selector:
    app: mysql
